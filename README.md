# Operativsystem
Assignments for the OS course ID1206. 
* In MallocAndFree the C library functions malloc() and free() are implemented.
* In GreenThreads a thread library is implemented containing: a scheduler, a context handler, mutex locks and conditional variables, much like the build in POSIX function pthread. 
